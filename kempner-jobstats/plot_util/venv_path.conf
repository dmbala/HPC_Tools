# jobstats_plot venv location. jobstats_plot runs $venv_env_path/.venv/bin/python
# (it re-execs into this venv on startup if plotext/rich aren't already importable),
# so no PATH or launcher setup is needed. Default: the shared kempner-jobstats
# deployment. setup/install_venv.sh rewrites the venv_env_path line below when you
# build a venv elsewhere.
venv_env_path=/n/holylfs06/LABS/kempner_shared/Everyone/cluster_scripts/job_eff/kempner-jobstats
